# Server
PORT=5000
NODE_ENV=production

# JWT — ganti dengan random string yang kuat di production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES=24h

# CORS — URL frontend
CLIENT_URL=http://localhost:3000
